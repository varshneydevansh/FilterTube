# FilterTube P0 Capture Fixture Traceability Current Behavior - 2026-05-19

Status: current-behavior proof. This is not an implementation patch.

This slice converts capture fixture traceability into a P0 stabilization family.
The raw root captures are important evidence, but they are not product source,
not release inputs, and not sufficient proof by themselves. A renderer, DOM
selector, endpoint, or mode claim becomes actionable only after a minimal
committed fixture and current-behavior test pin the exact path and side effects.

Current answer: the capture-to-fixture chain is explicit, but incomplete. The
workspace currently has 47 ignored capture entries, 46 unique paths, 45 present
paths, 44 committed reduced fixture fragments, and 31 represented raw source
families. Most high-risk watch, search, guide, comments, Shorts, Kids, YTM,
playlist, collaboration, and post capture families are still unextracted.

## Fixture Status

| P0 fixture | Current result | Evidence | Risk |
| --- | --- | --- | --- |
| `capture_traceability_main_watch_end_screen_dom_wall` | Partially extracted, but DOM wall is not satisfied today. | `YT_MAIN.json` is now partially extracted as mixed watch initial JSON plus later player metadata JSON capture evidence: fragment 0 proves watch rail `lockupViewModel` rows, a Shorts remix shelf, no-rule preservation, blocklist/whitelist decisions, `hideAllShorts`, continuation preservation, and map side effects, while also proving it is not Main browse/search completion proof. Fragment 1 is now extracted as metadata-only `/player` proof: `videoDetails`, `playerMicroformatRenderer`, cards, and endscreen element siblings pass through no-rule, disabled, blocklist, and whitelist modes while owner/meta map side effects still fire; it is not direct recommendation-filtering authority. `YT_MAIN_WATCH.html` is now partially extracted for embedded `ytInitialData[1]` playlist-panel and player-overlay end-screen JSON behavior, but it still has zero rendered player DOM end-screen selector tokens and remains unextracted for watch DOM wall proof. `YT_MAIN_NEXT.json` is now partially extracted as direct JSON comment-reload evidence, not watch/autoplay proof. `watchpage.json` is now partially extracted only for embedded `FEwhat_to_watch` rich-grid `postRenderer` rows, reclassified as browse/feed post evidence rather than watch-next evidence; `extracted_watch_paths.txt` is now classified as a derived watch path dump rather than JSON or DOM behavior proof. `tests/runtime/fixtures/captures/playlist-player-endscreen-dom.html` is reduced from `playlist.html` and proves rendered desktop player end-screen DOM exists there, but the source is an already-mutated playlist capture and not clean Main watch DOM wall proof; no committed clean Main watch end-screen DOM wall fixture exists. | End-screen/player DOM fixes could rely on memory instead of real watch DOM evidence. |
| `capture_traceability_main_next_compact_autoplay_renderer` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/main-watch-autoplay-video-endpoint.json` is reduced from `get_watch?prettyPrint=false.json` and proves the raw watch autoplay shape uses `autoplayVideo`/`nextButtonVideo` endpoint objects, not `compactAutoplayRenderer`; those endpoint objects still point to the same video after the matching supported playlist row is removed in blocklist and whitelist nonmatch modes. `tests/runtime/fixtures/captures/main-upnext-feed-watchpage-lockup-continuation.json` is reduced from `YT_MAIN_UPNEXT_FEED_WATCHPAGE.json` and proves a prose-plus-balanced-fragments `/youtubei/v1/next` `watch-next-feed` continuation with `lockupViewModel` rows, title/channel decisions, Mix identity boundaries, and zero parsed compact-autoplay, autoplay endpoint, end-screen, or playlist-panel keys. `tests/runtime/fixtures/captures/main-upnext-feed-watchpage2-claim-prefaced-lockup-continuation.json` is reduced from `YT_MAIN_UPNEXT_FEED_WATCHPAGE2.json` and proves a claim-prefaced `/youtubei/v1/next` `watch-next-feed` continuation with two primary decorated-avatar video lockups, blocklist/whitelist sibling preservation, map side effects, and no parsed compact-autoplay, autoplay endpoint, end-screen, playlist-panel, or collaborator roster keys; the prose collaborator claims are not parsed collaborator proof. `tests/runtime/fixtures/captures/main-upnext-feed-watchpage3-autoplay-previous-end-screen.json` is reduced from `YT_MAIN_UPNEXT_FEED_WATCHPAGE3.json` and proves an embedded `ytInitialData` watch payload with `autoplayVideo`/`nextButtonVideo`/`previousButtonVideo` endpoint objects, player-overlay end-screen rows, and no `compactAutoplayRenderer`. `tests/runtime/fixtures/captures/main-next-reload-modern-comments.json` is reduced from `YT_MAIN_NEXT.json` and proves that raw file is comment-reload evidence with zero parsed compact-autoplay or autoplay endpoint keys. No literal `compactAutoplayRenderer` fragment has a committed fixture. | Watch autoplay/end-screen leaks can remain unproven when endpoint, continuation lockup, and renderer shapes are treated as one gap. |
| `capture_traceability_main_search_no_rule_real_capture_pass_through` | Partially extracted, not satisfied today. | `YT_MAIN.json` is no longer counted as browse/search proof because its parseable fragment is watch initial JSON. `tests/runtime/fixtures/captures/main-search-refinement-card-renderer.json` is reduced from `strange_ytInitialData.json`; `docs/audit/FILTERTUBE_MAIN_SEARCH_REFINEMENT_CARD_CURRENT_BEHAVIOR_2026-05-24.md` and `tests/runtime/main-search-refinement-card-current-behavior.test.mjs` prove one mobile search refinement album card currently passes through matching keyword/channel rules and whitelist nonmatch mode because `searchRefinementCardRenderer` has no direct JSON rule coverage, while a supported watch-card sibling can be removed and seed search fetch still parses/rebuilds for harvest-only, active-rule, whitelist, and disabled paths. `tests/runtime/fixtures/captures/main-search-universal-watch-card-renderer.json` is reduced from the same raw source and proves one mobile search hero `universalWatchCardRenderer` wrapper filters by captured title/channel identity while direct child watch-card renderers remain separate gaps. `tests/runtime/fixtures/captures/main-search-direct-watch-card-rich-hero-subrenderers.json` is reduced from the same raw source and presents real nested `watchCardRichHeaderRenderer` and `watchCardHeroVideoRenderer` child objects as direct top-level renderer keys to prove those direct forms pass through matching blocklist and whitelist-nonmatch modes today; the raw source still has no `watchCardRHPanelVideoRenderer` token. `tests/runtime/fixtures/captures/main-search-compact-channel-renderer.json` is reduced from the same raw source and proves one mobile search `compactChannelRenderer` passes through blocklist keyword/channel and whitelist non-match modes while still queuing one channel-map side effect. `tests/runtime/fixtures/captures/main-home-rich-lockup-mix-renderer.json` and `tests/runtime/fixtures/captures/main-home-rich-video-renderer.json` are reduced from `logs.json`; `docs/audit/FILTERTUBE_MAIN_HOME_RICH_GRID_MIX_VIDEO_CURRENT_BEHAVIOR_2026-05-24.md` and `tests/runtime/main-home-rich-grid-mix-video-current-behavior.test.mjs` prove one Home Mix `lockupViewModel` filters by title but not display-only creator channel identity, one Home `videoRenderer` filters by title/canonical channel while queuing map side effects, sibling blocklist/whitelist behavior separates Mix and video authority, and home browse fetch still parses/rebuilds for harvest-only, active-rule, whitelist, and disabled paths. | Empty-install/no-rule performance fixes could still rely only on synthetic payloads for Main grid/channel rows unless additional real search/home sibling-set fixtures are extracted. |
| `capture_traceability_main_guide_no_rule_real_capture_pass_through` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/main-guide-entry-renderer.json` is reduced from `guide?prettyPrint=false.json` and proves one guide subscription channel row currently passes through matching keyword/channel rules even though it carries a browse ID and canonical handle. `docs/audit/FILTERTUBE_MAIN_GUIDE_ENDPOINT_NO_WORK_BOUNDARY_CURRENT_BEHAVIOR_2026-05-23.md` and `tests/runtime/main-guide-endpoint-no-work-boundary-current-behavior.test.mjs` additionally prove the guide endpoint is not a zero-work path today: empty blocklist, whitelist, and guide DOM-control-only fetches call `processData()` and rebuild, while disabled fetch still parses and rebuilds. | Full guide no-work proof remains incomplete because guide/sidebar endpoint policy still lacks full section/container, route-scoped negative siblings, metric, cache-invalidation, and disabled/empty endpoint work proof. |
| `capture_traceability_comment_continuation_append_reload_replace` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/main-comment-append-entity-response.json` is reduced from `YT_MAIN_NEXT_RESPONSE_COMMENT.json` and proves one append continuation response shape: `hideAllComments` removes the visible `commentThreadRenderer` item, while keyword/channel rules currently ignore the paired `frameworkUpdates.entityBatchUpdate.commentEntityPayload` containing comment text and author identity. `tests/runtime/fixtures/captures/main-next-reload-modern-comments.json` is reduced from `YT_MAIN_NEXT.json` and proves one reload continuation shape: header slot is preserved, body modern `commentThreadRenderer` wrappers are removed under `hideAllComments`, continuation item remains, and modern ViewModel payload identifiers are not actionable by keyword rules. Replace classification, full sibling comments, and comment-only keyword text boundaries still need fixtures. | Comment continuation rewrites can leak, hide wrong scaffolding, or leave entity payload authority unmodeled. |
| `capture_traceability_reel_item_owner_identity` | Partially extracted, not implementation-ready. | `tests/runtime/fixtures/captures/main-reel-player-overlay-renderer.json` is reduced from `reel_item_watch?prettyPrint=False.JSON` and contains active Shorts overlay owner UC id, handle, and avatar evidence, but `reelPlayerOverlayRenderer` has no direct `FILTER_RULES` entry today. | Shorts/reel owner identity can exist in raw JSON while current runtime still depends on weaker learned maps, DOM fallback, or resolver paths for some decisions. |
| `capture_traceability_kids_browse_public_web_renderer_drift` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/kids-latest-compact-video-owner-extension.json` is reduced from `yt_kids_latest.json` and proves two adjacent public-web `compactVideoRenderer` siblings with `kidsVideoOwnerExtension` owner identity, blocklist removal, whitelist allow/no-match behavior, sibling preservation, and map side effects. `tests/runtime/fixtures/captures/kids-browse-malformed-fragment-compact-video.json` is reduced from the parseable browse fragment inside malformed `ytkids_browse?alt=json.json` and proves compact-video blocklist/whitelist behavior while `kidsSlimOwnerRenderer` owner rail entries remain visible. The older `tests/runtime/fixtures/captures/kids-compact-video-renderer.json` covers one `YT_KIDS.json` compact-video baseline. Complete Kids public-web proof still lacks route/mode breadth, DOM/native WebView parity, no-rule budgets, and owner-rail policy authority. | Kids renderer drift can be confused with native app WebView layout evidence. |
| `capture_traceability_ytm_dom_selector_guardrails` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/ytm-dom-video-card-with-menu.html` is reduced from `YTM-DOM.html` and proves one Music search DOM card where a canonical `/channel/UC...` owner link, video ID, native `ytm-menu-renderer`, and FilterTube quick-block anchor coexist. `tests/runtime/fixtures/captures/ytm-watch-player-dom.html` is reduced from `YTM-WATCH PLAYER.html` and proves rendered mobile watch/player DOM after FilterTube mutation: player shell, related hidden row, selected hidden playlist row, visible sibling row, and nonselected hidden sibling row. `tests/runtime/fixtures/captures/ytm-watch-playlist-panel-json.json` is reduced from `YTM.json` and proves three watch `playlistPanelVideoRenderer` JSON fragments filter in blocklist and whitelist modes while lacking the selected/current row from the DOM fixture. `tests/runtime/fixtures/captures/ytm-browse-channel-list-item-renderer.json` is reduced from `ytm_browse?prettyPrint=false.json` and proves two `channelListItemRenderer` rows pass through blocklist and whitelist modes while queuing channel-map side effects. `tests/runtime/fixtures/captures/ytm-logs-playlist-bottom-sheet-stale-identity.html` is reduced from `YTM-LOGS.txt` and proves one log-derived playlist card where the visible card owner is `@KillTony` but the observed injected bottom-sheet item carries `UCWFKCr40YwOZQx8FHU_ZqqQ` and label `@JerryRigEverything`. | YouTube Music DOM selectors and menu injection can still be overclaimed without settings-mode watch/player pass-through, whitelist selected/current-row policy, no-playback side-effect proof, disabled/no-work browse budgets, route-scoped negative guardrails, clean bottom-sheet trigger-card identity, teardown, and stale-menu sibling proof. |
| `capture_traceability_collab_homepage_avatar_stack_false_positive` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/main-collab-homepage-avatar-stack.html` is reduced from `collab_on_homepage.html` and proves one collaboration search/home DOM card where avatar-stack markup, display names, and a `@veritasium` handle exist while collaborator UC IDs are blank. `tests/runtime/fixtures/captures/main-collab-resolved-search-card-dialog.html`, `docs/audit/FILTERTUBE_MAIN_COLLAB_RESOLVED_SEARCH_CARD_DIALOG_CURRENT_BEHAVIOR_2026-05-24.md`, and `tests/runtime/main-collab-resolved-search-card-dialog-current-behavior.test.mjs` are reduced from `collab.html` and prove the contrasting already-mutated search-card form: four resolved collaborator UC IDs, a pending Google DeepMind block marker, avatar-stack markup, native action-menu markup, and a dialog roster with four `/channel/UC...` links. | Avatar-stack identity can still be over-trusted or under-trusted without full homepage sibling, clean negative sibling, whitelist, disabled/no-rule lifecycle, menu mutation, and source/effect fixtures. |
| `capture_traceability_post_menu_insertion_boundaries` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/ytm-dom-post-card-with-menu.html` is reduced from `YTM-DOM.html` and proves one mobile backstage/community post shape with `ytm-backstage-post-thread-renderer`, `yt-post-header`, author handle link, `/post/...` permalink, and native `ytPostHeaderHostActionMenu` without a FilterTube menu item already inserted. `tests/runtime/fixtures/captures/main-doms-mutated-main-dom.html` is reduced from `DOMs.html` and proves that `DOMs.html` is a mixed already-mutated Main DOM snapshot with quick-block/fallback-menu markers but no clean post/community selector, header, action-menu, or `/post/...` permalink. `tests/runtime/fixtures/captures/main-watchpage-embedded-post-renderer.json` is reduced from `watchpage.json` and proves embedded modern `postRenderer` rows pass through blocklist and whitelist modes while queuing channel-map side effects. `post_opt1_logs.txt` is still missing historical evidence. DOMs.html is now partially extracted for mixed already-mutated Main DOM classification; Main post/community insertion proof remains blocked. | Post quick/menu insertion behavior can still drift between native and fallback paths without Main post fixtures, negative sibling proof, native/fallback insertion side-effect proof, and no-rule lifecycle proof. |
| `capture_traceability_playlist_json_creator_identity` | Partially extracted, not satisfied today. | `tests/runtime/fixtures/captures/main-compact-radio-renderer.json` is reduced from `playlist.json` and proves one `compactRadioRenderer` Mix/radio card carries playlist/video IDs plus display-only YouTube byline, but not creator channel identity. `tests/runtime/fixtures/captures/playlist-json-player-metadata.json` is also reduced from `playlist.json` and proves the later `ytInitialPlayerResponse` fragment is current-video metadata for `Pkh8UtuejGw`/`ShawnMendesVEVO`, not playlist creator authority. `tests/runtime/fixtures/captures/playlist-panel-header-mix-dom.html` is reduced from `playlist.html` and proves the Mix header publisher says `Mixes are playlists YouTube makes for you`, has `playlist-type="RDUc"`, and has no `/channel/`, `/@`, `data-filtertube-channel-id`, selected-row state, or hidden markers; it is not creator channel identity proof. | Playlist creator/channel identity remains DOM-heavy or inferred unless a playlist header/metadata/video-list fixture proves canonical creator identity or an explicit no-creator policy. |

## Required Fixture Schema

Every future extraction should record:

```text
captureFixtureTraceability {
  rawSourceCapture
  minimalFixturePath
  fixtureKind: json | html | text
  sourceSurface
  route
  rendererPathOrDomSelector
  settingsMode
  expectedDecision
  sideEffects
  negativeSiblingVisibleFixture
  releaseInputAllowed: false
}
```

## Current Verdict

P0 capture fixture traceability is not implementation-ready. The existing
reduced fixtures are useful, but most raw evidence families still need minimal
committed fixtures before renderer, selector, endpoint, Kids, YTM, watch,
comments, playlist, or post behavior changes rely on them.

Playlist player end-screen DOM classification addendum: `docs/audit/FILTERTUBE_PLAYLIST_PLAYER_ENDSCREEN_DOM_CLASSIFICATION_CURRENT_BEHAVIOR_2026-05-23.md`, `tests/runtime/playlist-player-endscreen-dom-classification-current-behavior.test.mjs`, and `tests/runtime/fixtures/captures/playlist-player-endscreen-dom.html` pin that rendered desktop player end-screen DOM exists in `playlist.html`, but the source is an already-mutated playlist capture with 106 `filtertube-hidden` tokens, 53 `data-filtertube-hidden` tokens, 2 `aria-selected=true` tokens, and 0 `compactAutoplayRenderer` tokens; it is not clean Main watch DOM wall proof. The active gate still requires clean Main watch blocklist, whitelist, disabled/no-work, fullscreen, sibling-visible, no synthetic click, and no playback side-effect proof before player end-screen DOM optimization or JSON-first authority changes.

Compact autoplay raw corpus census addendum: `docs/audit/FILTERTUBE_COMPACT_AUTOPLAY_RAW_CORPUS_CENSUS_CURRENT_BEHAVIOR_2026-05-23.md` and `tests/runtime/compact-autoplay-raw-corpus-census-current-behavior.test.mjs` pin that current ignored raw evidence mentions `compactAutoplayRenderer` only in historical text notes and not in any raw JSON or HTML capture; raw JSON/HTML captures: 0, committed compact autoplay fixtures: 0. A real capture is still required before compact autoplay renderer support, player autoplay optimization, or JSON-first authority changes.

YTM compact playlist creator authority boundary addendum: `docs/audit/FILTERTUBE_YTM_COMPACT_PLAYLIST_CREATOR_AUTHORITY_BOUNDARY_CURRENT_BEHAVIOR_2026-05-23.md`, `tests/runtime/ytm-compact-playlist-creator-authority-boundary-current-behavior.test.mjs`, and `tests/runtime/fixtures/captures/ytm-compact-playlist-renderer.json` pin that canonical creator-looking fields are present in a real YTM `compactPlaylistRenderer`, but it remains unsupported/direct-gap and not first-class JSON filter authority. Blocklist title/channel rules, whitelist nonmatch, whitelist match, and `hidePlaylistCards` preserve the row while channel-map side effects still fire.

Playlist panel header Mix creator addendum: `docs/audit/FILTERTUBE_PLAYLIST_PANEL_HEADER_MIX_CREATOR_CURRENT_BEHAVIOR_2026-05-23.md`, `tests/runtime/playlist-panel-header-mix-creator-current-behavior.test.mjs`, and `tests/runtime/fixtures/captures/playlist-panel-header-mix-dom.html` pin that the playlist/Mix header fixture is not creator channel identity proof and not playlist creator identity proof. `playlist-selected-row.html` remains display-only byline proof, and `playlist.json` player metadata remains current-video metadata, not playlist header authority. A real playlist creator fixture is still required before JSON-first playlist creator filtering or playlist-owner optimization changes.

Playlist JSON player metadata boundary addendum: `docs/audit/FILTERTUBE_PLAYLIST_JSON_PLAYER_METADATA_BOUNDARY_CURRENT_BEHAVIOR_2026-05-23.md`, `tests/runtime/playlist-json-player-metadata-boundary-current-behavior.test.mjs`, and `tests/runtime/fixtures/captures/playlist-json-player-metadata.json` pin that `playlist.json` fragment 1 is current-video metadata, not playlist creator authority and not creator channel identity proof for the playlist. The compact radio fragment remains display-only YouTube byline proof, and the playlist creator fixture remains required before JSON-first playlist creator filtering or playlist-owner optimization changes.

TMP watch playlist collaborator dialog addendum: `docs/audit/FILTERTUBE_MAIN_WATCH_TMP_PLAYLIST_COLLAB_DIALOG_CURRENT_BEHAVIOR_2026-05-24.md`, `tests/runtime/main-watch-tmp-playlist-collab-dialog-current-behavior.test.mjs`, and `tests/runtime/fixtures/captures/main-watch-tmp-playlist-collab-dialog.json` pin that `tmp.json` is mixed browse `ytInitialData` plus later get-watch array evidence, not historical scratch. The reduced fixture proves a selected watch Mix playlist collaborator row where `UCGnjeahCJW1AF34HBmQTJ-Q` / `/@shakiraVEVO` is harvested from a title command, while current filtering uses renderer-context collaborator ID `UCYLNGLIzMhRTi6ZOLjAPSmw` for blocklist/whitelist decisions. This keeps parsed collaborator roster reconciliation open before JSON-first collaborator or playlist optimization changes.

First optimization metric collector fixture provenance matrix addendum: `docs/audit/FILTERTUBE_FIRST_OPTIMIZATION_METRIC_COLLECTOR_FIXTURE_PROVENANCE_MATRIX_CURRENT_BEHAVIOR_2026-05-24.md` and `tests/runtime/first-optimization-metric-collector-fixture-provenance-matrix-current-behavior.test.mjs` maps these P0 capture fixture traceability gaps into first-collector fixture provenance requirements. The addendum pins 12 collector fixture provenance rows, 12 route/surface obligations covered, 10 candidate binding rows covered, 6 evidence fixture/parity rows covered, 8 required fixture/parity fields covered, 11 P0 capture traceability rows covered, 46 unique raw capture obligation paths covered, 0 runtime collector fixture packets approved, and 0 implementation-ready fixture provenance rows. It does not close this traceability gate: raw captures and reduced fixtures still need scoped positive/negative/no-rule/disabled/parity packets before renderer, selector, endpoint, Kids, YTM, watch, comments, playlist, or post optimization changes rely on them.

## Method Semantic Proof Gap Boundary

`docs/audit/FILTERTUBE_METHOD_SEMANTIC_PROOF_GAP_INDEX_CURRENT_BEHAVIOR_2026-05-25.md`
is a required source input before this P0 capture fixture traceability surface
can support runtime optimization. Current proof pins:

```text
method semantic proof gap files covered: 69
method semantic proof gap lexical callables covered: 5736
files with complete per-callable semantic proof: 0
lexical callables requiring semantic proof before behavior changes: 5736
affected callable semantic proof: NO-GO
runtime behavior changed: no
```

These counts are audit-only blockers. They do not approve runtime
optimization, JSON-first behavior, fixture packet approval, raw-corpus
extraction, whitelist behavior changes, or selector/renderer authority changes.
